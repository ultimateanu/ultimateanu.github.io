// Place your settings in this file to overwrite default and user settings.
{
  "files.autoSave": "afterDelay",
  "files.exclude": {
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.js.map": true
  },
  "editor.fontSize": 16
}